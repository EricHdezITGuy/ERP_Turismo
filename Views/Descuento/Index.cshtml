@model IEnumerable<AdminTurismoERP.Descuento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreDescuento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantidadDescuento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Codigo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreDescuento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadDescuento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Codigo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DescuentoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DescuentoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DescuentoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
